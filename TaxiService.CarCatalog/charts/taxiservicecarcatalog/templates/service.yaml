apiVersion: v1
kind: Service
metadata:
  name: {{ template "taxiservicecarcatalog.fullname" . }}
  labels:
    app: {{ template "taxiservicecarcatalog.name" . }}
    chart: {{ template "taxiservicecarcatalog.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "taxiservicecarcatalog.name" . }}
    release: {{ .Release.Name }}
